#!/usr/bin/env bash

source "$HOME/.envs.site.sh"

case "$(hostname)" in
roozbeh-*)
    # home machine — use OpenRouter
    echo "$OPENROUTER_API_KEY"
    ;;
*)
    # company laptop — use Snapp LLM gateway
    echo "$SNAPP_AI_API_KEY"
    ;;
esac
