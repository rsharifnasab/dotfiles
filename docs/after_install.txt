+ after install arch-base distro, customize `post_install.sh` and run it (inside `install` folder)
+ add [chaotic-aur](https://aur.chaotic.cx/) to pacman
+ add "persian with persian keyboard" keyboard layout
+ change "change layout option" to "caps lock"
+ check super key
+ check zsh backspace
+ check ctrl+alt+t
+ check screenshot keybinding
+ check alt+f4 key binding
+ change default apps
+ setup shecan
+ check bluetooth and sound
+ login to sites
   + gmail
   + github (setup copilot with `CoPilot setup`)
   + wakatime (get api key from https://wakatime.com/settings/account)
+ setup firefox sync
+ setup telegram account
+ change telegram setting
    + auto-download settings
    + scaling
    + launch when system start
    + notification
    + send with ctrl-enter

+ bring ssh keys to ~/.ssh
+ clone `~/proj` files if needed

+ customize taskbar
+ in xfce set super+space to: xfce4-popup-whiskermenu
+ set f12 to: `xfce4-terminal --drop-down` or `guake`
+ check docker installation: run `dockerd` and in another terminal run: `docker run hello-world`

+ restart system!
